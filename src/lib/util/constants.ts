export const exportPngErrorMsg = `Couldn't export PNG.
It's possible that you've included an image from a source that doesn't have CORS permissions set.
Try using an image from imgur or another source that allows CORS. Unfortunately there are security reasons
that prevent exporting a canvas with images from servers that don't have CORS permissions set.`;
